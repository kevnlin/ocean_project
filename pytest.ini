[pytest]
# experiments/real_data/50_prospective_test.py matches pytest's default
# *_test.py pattern. It is the P7 frozen prospective evaluation, not a test,
# and importing it runs its argparse, which crashed a bare `pytest` at
# collection. Collect only the real suite.
testpaths = tests
